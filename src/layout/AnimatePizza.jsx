import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

gsap.registerPlugin(ScrollTrigger);

const AnimatePizza = () => {
    return (
        <div className="w-full">
            {/* The orange empty part was removed. Add new content here. */}
        </div>
    )
}

export default AnimatePizza;